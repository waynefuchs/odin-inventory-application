extends layout

block content
  h1= title
    a.button(href="/items") Back

  p #{item.name}
  p #{item.description}
  p #{item.category}
  p #{item.price}
  p #{item.available}

  h2 Are you sure you want to delete this item?

  form(method='post' action='')
    button Delete
