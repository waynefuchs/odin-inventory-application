extends layout

block content
  h1= title
    nav
      a(href="/") Home
      a(href="/items") Back


  p #{item.name}
  p #{item.description}
  p #{item.category}
  p #{item.price}
  p Quantity: #{item.available}